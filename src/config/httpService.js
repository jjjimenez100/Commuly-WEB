// Temporarily hardcode .env variable while still figuring out a way to add it to the amplify environment
const BASE_URL =
  'http://api.commuly.com:3000/commuly/api/v1/' || 'http://localhost:5000/commuly/api/v1';

export default BASE_URL;
